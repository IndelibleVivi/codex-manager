# Codex 管理器 · 使用教学

Codex 管理器会直接读取和修改你电脑上的 `~/.codex`。它能让隐藏的配置、
Skills、记忆、历史记录和临时文件变得可见，也能执行编辑、删除、恢复和批量
清理。

先记住一句：**分类帮你定位，决定仍然要你自己做。** “临时”“历史”“系统
托管”是当前程序根据路径做的启发式判断，不能保证某个文件对你毫无价值。

## 一、安装

当前打包以 Windows 为目标。

1. 安装 Python 3。
2. 安装时勾选 `Add python.exe to PATH`。
3. 下载或 clone 本仓库。
4. 打开 `Codex管理器` 文件夹。
5. 双击 `① 双击我安装.bat`。
6. 桌面会出现“Codex 管理器”和“Codex 库存仪表盘”两个快捷方式。

双击“Codex 管理器”后，本机会启动一个只监听 `127.0.0.1` 的网页界面，并在
浏览器中打开。不要把它的端口转发到局域网、公网或 Tunnel。

## 二、文件管理

左侧会把内容分成配置、Skills、记忆、临时、历史、系统托管和其他等类别。
点目录可以展开，点文本文件可以在右侧查看。

当前限制：

- 常见 Markdown、JSON、YAML、TOML、rules、TXT 和配置文本可查看；
- 文件超过 3 MiB 时不打开；
- 文件超过 512 KiB 时只读，不能在网页里编辑；
- `.git` 内部文件保持只读；
- 非文本文件不通过管理器编辑。

### 编辑

点击编辑、保存后，原文件会立即被 UTF-8 文本覆盖。**编辑没有自动备份。**
对 `AGENTS.md`、`config.toml`、rules、Memory 和重要 Skills，先确认有手动备份
或版本控制。

发现乱码、`U+FFFD` 替换字符或文件编码不明确时，不要保存。当前编辑器不适合修改
非 UTF-8 文件，保存可能造成不可逆重编码。

### 删除

普通删除不会立刻消失，会移动到工具目录里的：

```text
零件箱/隔离区（删错了来这捞）/
```

在“回收站”页可以恢复。原位置已有同名文件时，工具会拒绝覆盖。

隔离项目超过 7 天后，会在回收站被打开/扫描时自动彻底删除。点击“彻底删”
或“全部清空”会立即删除，无法恢复。

删除整个 Codex 管理器安装文件夹也会一起删除隔离区。卸载前先检查有没有要捞
回去的内容。

## 三、一键清理

批量清理目前只接受两类：

- 临时垃圾；
- 历史记录。

它会移动 `~/.codex` 根目录下命中该分类的顶层项目到隔离区。清理历史可能
导致旧会话、附件、生成图片、可视化或 dictation 记录无法继续访问。

操作前先看具体路径。Codex 或插件升级后可能出现程序还不认识的新目录，
“其他”也不等于垃圾。

## 四、Markdown 查重

“md 查重”按完整文件内容寻找完全一样的 Markdown：

- 跳过超过 2 MiB 的文件；
- 最多展示 100 组；
- 只判断字节完全相同；
- 不判断语义相似；
- 不自动替你选择应该删除哪一份。

重复文件也可能分别属于不同项目或历史节点，删除前要看路径。

## 五、新建 Skill

输入一个由小写字母、数字和连字符组成的名称，长度 2–41。工具会创建：

```text
~/.codex/skills/<skill-name>/SKILL.md
```

同名目录存在时会拒绝覆盖。保存后 Skill 会立即出现在本机配置中；是否被
Codex 正确调用，还取决于 description、当前任务和其他配置。

## 六、翻译

翻译功能需要本机运行 OpenAI-compatible 代理：

```text
http://127.0.0.1:10100/v1/chat/completions
```

没有代理时，翻译失败，其他功能不受影响。

“127.0.0.1”只表示先把请求交给本机代理。代理后面可能调用云模型，所以所选
文本仍可能离开电脑。不要翻译 API key、token、账号信息、私密聊天、申请材料
或不愿交给上游 provider 的内容。

单次翻译上限为 20,000 个字符。

## 七、库存仪表盘

“Codex 库存仪表盘”运行独立 PowerShell 扫描，帮助发现目录数量和体积异常。
红灯适合提醒你回来检查，它不等于可以直接批量删除。

## 八、减少以后继续堆积

可以在 user-level 或项目自己的 `AGENTS.md` 中加入类似规则：

```md
## 文件卫生

- 一次性脚本、下载物、草稿和中间产物放进系统临时目录或项目明确的 ignored
  workspace，不要散落在项目根目录或 `~/.codex`。
- 任务结束前区分交付物、可复用 fixture、缓存和临时产物；只删除已确认没有
  后续价值的内容。
- 私人 handoff / working continuity 放在明确的本地私有目录，永不跟随
  catch-all staging 进入 remote。
- commit 前检查 `git status` 和 staged diff，不混入临时文件、secrets、缓存
  或私人 continuity。
```

## 九、卸载

1. 关闭 Codex 管理器的命令行窗口/进程。
2. 打开回收站，恢复还需要的内容。
3. 删除桌面快捷方式。
4. 删除 `Codex管理器` 文件夹。

工具不会额外安装后台服务。删除文件夹后，文件夹内尚未恢复的隔离内容也会
一起消失。
